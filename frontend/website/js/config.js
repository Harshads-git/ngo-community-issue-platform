// Configuration Variables for the Frontend
// Change this URL when deploying the backend to a cloud service (e.g. Render, Railway, Heroku)
const CONFIG = {
    API_BASE_URL: 'https://ngo-community-issue-platform.onrender.com'
};
